#include "main.h"

/**
 * swap_int - swaps the values of two integers
 * @a: pointer to an int
 * @b: pointer to an int
 * Returns: Nothing
 */

void swap_int(int *a, int *b)
{
	int c = 0;

	*a = c;
	*a = *b;
	*b = c;
}
