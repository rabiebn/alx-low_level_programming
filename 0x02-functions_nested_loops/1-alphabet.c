#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0.
 */

void print_alphabet()
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	return;
}
int main(void)
{
	return (0);
}
